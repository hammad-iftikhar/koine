pre-commit:
  commands:
    check:
      glob: "*.{ts,tsx,json,jsonc,css}"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    types:
      run: pnpm types
